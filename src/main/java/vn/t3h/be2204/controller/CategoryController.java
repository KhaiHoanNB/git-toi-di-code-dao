package vn.t3h.be2204.controller;

import org.springframework.stereotype.Controller;

@Controller
public class CategoryController {
    // 1)Tạo bảng category(ID, NAME, DESCRIPTION) trong db
    // 2) tạo CategoryDto và controller thêm mới, chi tiết , danh sách category
}
